{
  "name": "@harness-profiler/opencode",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "OpenCode LM-middleware capture for harness-profiler (§4.2-B)",
  "keywords": ["opencode", "harness-profiler", "tracing", "lm-middleware"],
  "dependencies": {
    "@agent-profiler/schema": "workspace:*"
  },
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit"
  }
}
