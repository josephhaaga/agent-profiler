{
  "name": "@agent-profiler/server",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@agent-profiler/schema": "workspace:*",
    "@agent-profiler/store": "workspace:*"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bunx tsc -p tsconfig.json",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit"
  }
}
